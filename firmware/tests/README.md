Cross-project and hardware-oriented test artifacts live here.

- `hardware-bench`: real hardware test procedures and fixtures
- `system`: end-to-end and integration test definitions
- `soak`: long-run validation plans and logs

The authoritative test scope and pass criteria are defined in `../../docs/specifications/verification-and-acceptance.md`.
