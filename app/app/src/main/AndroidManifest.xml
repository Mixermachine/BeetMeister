<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.bluetooth_le"
        android:required="false" />

    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" android:maxSdkVersion="30" />
    <uses-permission
        android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" tools:targetApi="s" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".BeetMeisterApplication"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.BeetMeister">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.BeetMeister">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <service
            android:name=".service.BeetMaintenanceForegroundService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />
        <!--
            Test-only broadcast hook. Fired by the test-harness
            orchestrator's firmware_update dispatch via
            adb shell am broadcast, BEFORE the test's
            am instrument launches the app. Clears the
            phone-side BLE bond for the controller so the
            app's auto-connect triggers a fresh Just Works
            pair instead of trying to encrypt with a stale
            link key (which would fail with HCI_ERR_KEY_MISSING
            and stall the test). See ClearBleBondReceiver.kt
            for the full rationale and production-safety
            analysis.
        -->
        <receiver
            android:name=".debug.ClearBleBondReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="de.aarondietz.beetmeister.debug.action.CLEAR_BLE_BOND" />
            </intent-filter>
        </receiver>
    </application>

</manifest>
