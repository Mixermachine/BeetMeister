Android application sources for BLE-based controller access live here.

- `app`: Kotlin + Compose application module

The app-facing protocol and behavior specification lives centrally in `../docs/specifications/ble-and-android-app.md`.
Keep this tree code-focused unless a short module-local note becomes necessary.
