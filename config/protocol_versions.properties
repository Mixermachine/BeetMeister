# WARNING: The maintenance protocol is intended to be effectively frozen.
# Do not change maintenance command names, required fields, field meanings,
# compact aliases, or wire behavior casually.
# If a change is ever unavoidable, preserve backward compatibility for every
# shipped maintenance protocol version and coordinate app + firmware together.
maintenance_protocol_version=1
runtime_protocol_version=10
